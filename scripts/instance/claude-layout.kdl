// Zellij layout for Claude Code instances.
// The startup script handles auth if credentials don't exist.
// Tab switching managed by the web UI via agent IPC.
layout {
    tab name="shell" focus=true {
        pane command="/home/claude/startup.sh"
    }
}
